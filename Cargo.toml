[package]
name = "pop65"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.17", features = ["derive", "wrap_help"] }

[dev-dependencies]
criterion = "0.5.1"

[[bench]]
name = "test_s"
harness = false

[profile.release]
debug = 1
